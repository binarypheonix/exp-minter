import { address, BigMap, mutez, nat, unit } from './type-aliases';
declare type Storage = {
    allowlist: BigMap<address, unit>;
    market_storage: {
        admin?: {
            admin: address;
            paused: boolean;
            pending_admin?: address;
        };
        sales: BigMap<nat, {
            seller: address;
            sale_data: {
                sale_token: {
                    fa2_address: address;
                    token_id: nat;
                };
                price: mutez;
                amount: nat;
            };
        }>;
        next_sale_id: nat;
    };
};
declare type Methods = {
    confirm_admin: () => Promise<void>;
    pause: (param: boolean) => Promise<void>;
    set_admin: (param: address) => Promise<void>;
    buy: (param: nat) => Promise<void>;
    cancel: (param: nat) => Promise<void>;
    sell: (fa2_address: address, token_id: nat, price: mutez, amount: nat) => Promise<void>;
    update_allowed: (param: BigMap<address, unit>) => Promise<void>;
};
export declare type FixedPriceSaleMarketTezAllowlistedContractType = {
    methods: Methods;
    storage: Storage;
    code: {
        __type: 'FixedPriceSaleMarketTezAllowlistedCode';
        protocol: string;
        code: object[];
    };
};
export {};
