import { address, BigMap, bytes, contract, MMap, nat, unit } from './type-aliases';
declare type Storage = {
    admin: {
        admins: Array<address>;
        paused: boolean;
        pending_admins: BigMap<address, unit>;
    };
    assets: {
        ledger: BigMap<nat, address>;
        next_token_id: nat;
        operators: BigMap<{
            0: address;
            1: address;
            2: nat;
        }, unit>;
        token_metadata: BigMap<nat, {
            token_id: nat;
            token_info: MMap<string, bytes>;
        }>;
    };
    metadata: BigMap<string, bytes>;
};
declare type Methods = {
    confirm_admin: () => Promise<void>;
    pause: (param: boolean) => Promise<void>;
    remove_admin: (param: address) => Promise<void>;
    set_admin: (param: address) => Promise<void>;
    balance_of: (requests: Array<{
        owner: address;
        token_id: nat;
    }>, callback: contract) => Promise<void>;
    transfer: (param: Array<{
        from_: address;
        txs: Array<{
            to_: address;
            token_id: nat;
            amount: nat;
        }>;
    }>) => Promise<void>;
    add_operator: (owner: address, operator: address, token_id: nat) => Promise<void>;
    remove_operator: (owner: address, operator: address, token_id: nat) => Promise<void>;
    mint: (param: Array<{
        token_metadata: {
            token_id: nat;
            token_info: MMap<string, bytes>;
        };
        owner: address;
    }>) => Promise<void>;
};
export declare type Fa2MultiNftAssetMultiAdminContractType = {
    methods: Methods;
    storage: Storage;
    code: {
        __type: 'Fa2MultiNftAssetMultiAdminCode';
        protocol: string;
        code: object[];
    };
};
export {};
